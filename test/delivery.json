{
    "notifiers": [
        {
            "type": "CliIdNotifier",
            "options": {
                "ON_KEY_PRESENTED_URL_TEMPLATE": "http://mealplan.impulsreha.local:8000/essen/{Id}"
            }
        }
    ],
    "listeners": [
        {
            "type": "CogDBusCtlListener",
            "options": {
                "ON_KEY_PRESENTED_URL_TEMPLATE": "http://mealplan.impulsreha.local:8000/essen/{Id}"
            }
        }
    ],
    "device": "/dev/ttyUSB1"
}